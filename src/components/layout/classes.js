import { makeStyles } from '@material-ui/core/styles';

const classes = makeStyles((theme) => ({
  root: {
    flexGrow: 1,
  },
  menuButton: {
    position: 'absolute',
  },
  title: {
    flexGrow: 1,
    textAlign: 'center',
  },
}));

export default classes;
